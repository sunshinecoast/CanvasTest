<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <declare-styleable name="QuestionnaireStyle">
        <!-- Customize your theme here. -->
        <attr name="default_width" format="dimension"/>
        <attr name="default_height" format="dimension"/>
        <attr name="background_color" format="color"/>
        <attr name="answer_error_color" format="color"/>
        <attr name="answer_color" format="color"/>
        <attr name="select_color" format="color"/>
        <attr name="text_size" format="dimension"/>
        <attr name="text_color" format="color"/>
        <attr name="text_left_margin" format="dimension"/>
        <attr name="text_right_margin" format="dimension"/>
    </declare-styleable>

</resources>
